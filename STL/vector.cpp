#include<bits/stdc++.h>
#include<vector>

using namespace std;

int main(){
#ifndef ONLINE_JUDGE
    freopen("input.txt","r",stdin);
    freopen("output.txt","w",stdout);
#endif

    vector<int> v;

    int n;
    cin>>n;
    for(int i=0;i<n;i++){
        int no;
        cin>>no;
        v.push_back(no);
        cout<<"Capacity: ";
    }

    for(int i=0;i<v.size();i++){
        cout<<v[i]<<" ";
    }
    








    return 0;
}